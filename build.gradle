
plugins {
  id "java"
  id "com.google.protobuf" version "0.8.12"
}

repositories {
  mavenCentral()
}

protobuf {
  generateProtoTasks {
    all().each { task ->
      task.builtins {
        // If you wish to avoid generating Java files:
        remove java
      }
    }
  }
}
